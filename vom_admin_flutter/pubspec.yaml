name: vom_admin
description: "V.O.M 관리자 앱 - 실시간 학습 모니터링 및 위험 알림"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Supabase
  supabase_flutter: ^2.8.0

  # 진동 피드백
  vibration: ^2.0.0

  # 로컬 알림
  flutter_local_notifications: ^18.0.1

  # 상태 관리
  provider: ^6.1.2

  # 저장소
  shared_preferences: ^2.3.4

  # URL 런처 (전화 걸기)
  url_launcher: ^6.3.1

  # NFC 태그 읽기
  flutter_nfc_kit: ^3.5.2

  # 클립보드
  clipboard: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
